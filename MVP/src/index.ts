import chalk from 'chalk';
import { pluralizeText } from './Utils/utils';
console.log(`🌟 Everything appears to be ${chalk.greenBright('Operational')}! 🌟`);
console.log(pluralizeText([1], 'test'));
